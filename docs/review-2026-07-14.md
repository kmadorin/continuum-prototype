# Continuum prototype — frontend work review

**Branch:** `ui-ux` (PR [#3](https://github.com/kmadorin/continuum-prototype/pull/3)) · **Scope:** web app (`app/web`), anchored documents (`app/custody/docs`), pitch deck (`pitch/deck.html`) · **Date:** 2026-07-14 – 15

---

## TL;DR

- **Built a full local sandbox** so the entire six-seat demo runs with no devnet, no keys, and no secrets: the real custody backend runs against an in-memory fake ledger that faithfully reproduces Canton privacy projection, choice authorization, and the atomic Close. `npm run dev:sandbox`, sign in as `<role>` / `<role>-demo`.
- **Audited all six seats, then fixed what the audit found**, in order of severity: the demo economics now compose (every seat is paid exactly what its screen promises; unit conservation holds at close), and GP/LPAC oversight counts **contentless markers** instead of polling for private contracts it can never read.
- **Executed a complete institutional redesign** of the product UI — new design system (dark surfaces, one indigo action color, green only for confirmed on-ledger facts, serif/grotesk type pairing, tabular numerals, no monospace), a persistent-sidebar app shell, and a component system — while preserving every workflow, permission, term, and status. All 73 web tests stayed green throughout.
- **Rebuilt the sign-in as a product face**: brand panel with value proposition, animated flow field with a cursor spotlight, aurora glow, per-role "open in a new window" deep links, and an internal `/pitch-deck` route.
- **Restyled the anchored documents** (valuation report, fairness opinion, PSA) to the same design system and regenerated their on-ledger sha256 manifest so `/verify` keeps matching.
- **Strengthened the pitch deck** with the close's real numbers and a self-contained demo slide.
- **~50 rapid design-polish iterations** (motion, spacing, responsive, icons, controls), each verified with Playwright screenshots against the running sandbox.
- **One-click seats**: the credential form is hidden for now (`SHOW_LOGIN_FORM` flag) — a seat row, or its `/?seat=<role>` new-window link, signs straight in with the demo credentials.
- **Systematic animation review** against the design-engineering skill: every easing, duration, entrance and press state audited; deviations fixed (see §5a).
- **Merged `main`'s parallel simplification pass** (94d10b7) with deliberate, documented resolutions — the PR is mergeable and green.
- **Verification:** 74 web tests + 37 backend tests green; end-to-end money-flow click-through from every seat; deterministic sandbox keys so sessions survive dev-server restarts.
- **Deferred (recorded, needs Daml/redeploy or a naming decision):** LPAC valuation visibility, deal naming, a close entry in the activity feed, the unreachable LPACConsentRequest→Grant path.

---

## 1. Local sandbox — the whole demo without a devnet (`5d97bc9`, `e4d647c`)

**Why.** The devnet party keys were unavailable, and frontend work needed a fast, safe loop.

**What.** The real Hono `createApp(deps)` backend is booted inside the Vite dev server with three ports swapped: the signer, the fetch implementation, and the read path all point at an in-memory `FakeLedger`. The fake is not a stub — it reproduces the properties the demo exists to show:

- **Privacy projection**: `projectionOf()` computes each party's view from the same signatory/observer rules as the Daml templates, so a seat literally cannot read a contract the ledger would not hand it.
- **Choice authorization and consumption**: controllers are enforced; consuming choices consume.
- **Atomic Close**: the full leg set (ValidateIssuance, transfers, burns, disclosures) executes against a snapshot with a unit-conservation assert — any failure rolls the whole transaction back.

Tenant keys are **deterministic** (`sha256('continuum-sandbox-' + role)` → mnemonic), so party ids are identical across dev-server restarts and a signed-in browser tab keeps working. This fixed the "metrics never load" bug class where a stale session cookie pointed at a party that no longer existed.

## 2. Audit, then correctness (`5535311`, `9f69424`, `95c6b8d`, `6203ba6`)

A full click-through audit of all six seats is recorded in `docs/ui-audit-2026-07-14.md`, and the fixes landed in the order the user set: money first, oversight second, copy/interaction last.

- **The cap table composes** (`9f69424`). The demo economics are now one consistent story: reference NAV $500M = $300M exiting + $200M rolling; at the 96% clearing price the buyer funds $288M cash and receives 288M CV units, rollers receive 192M units, and `288M + 192M = 480M = psaPrice` — the exact conservation the on-ledger Close asserts. Every seat's screen shows numbers that add up to what actually settles.
- **Oversight without leaking** (`95c6b8d`). The GP is not a stakeholder of a `SealedBid` or an `LPElection`; a page that polls for them counts zero forever. Oversight now runs on the **contentless markers** (`BidFiled`, `ElectionFiled`) that carry no amounts — the GP reads "2 of 2 responded" with zero private contracts in its projection, and tests pin that the page never even asks the ledger for a template it cannot see.
- **Copy stops contradicting the ledger** (`6203ba6`). The Valuer seat no longer claims an audience its `observer gp` projection does not give it; stage hints, gate copy, and post-close states were aligned with what is actually on-ledger.

**Deferred by decision** (recorded in the audit doc): LPAC valuation visibility (a one-line Daml observer change + redeploy) and the deal-naming question (Meridian CV I vs Project Continuum CV I, L.P.). Two cosmetics remain open: the activity feed lacks a close entry, and the `LPACConsentRequest → Grant` path is unreachable in the current flow.

## 3. Institutional redesign (`85bad3b` and the polish train)

A ground-up visual system, applied product-wide with all functionality, terminology, roles, permissions, and statuses preserved:

- **Tokens.** Five dark neutral surfaces, thin borders (dimmed twice on request — final `#232B36 / #2F3947 / #1B212A`), one indigo action color `#4F67E8` with an indigo→violet support ramp, and a strict semantic rule: **green appears only on confirmed on-ledger facts** (settled, anchored, recorded) — never decoratively.
- **Type.** Signifier (rare, editorial — page titles, the deal name) over Söhne (all functional UI), loaded as Newsreader + Geist. **No monospace anywhere**: data alignment comes from tabular numerals, not a second family.
- **App shell.** Persistent sidebar (brand + deal context + section nav as a real WAI-ARIA vertical tablist + signing identity/custodian footer), sticky translucent page header, full-width content. The deal-status slot in the sidebar never renders empty: skeleton pill → stage chip → a quiet dash when the seat's projection has no deal.
- **Components.** Cards/panels, flat status chips (fill only — no borders, no dots; state reads from color + label, and a chip never stretches to fill a column), KPI tiles with as-of lines, key-value tables, a bounded `SliderField` (big slider target + exact numeric entry + min/max caps — replacing tiny spinner buttons), redaction bars for sealed values, and the "cannot see" privacy note as a product feature, not an apology.

## 4. Sign-in as the product's face (`ce58a0f`, `8025692`, and this batch)

- Brand panel: eyebrow, serif headline, one-sentence description with a deliberate line break, five USP bullets, CTAs ("Enter demo workspace", "See how it works" → the internal `/pitch-deck` route that recreates the deck in-repo).
- **Flow field**: barely-there interweaving SVG streams; a brighter copy of the same lines sits under a **cursor-following radial mask**, so the streams glow where the pointer travels (rAF-eased attribute writes, zero React re-renders; lights up fast, fades out slowly when the cursor leaves).
- **Aurora**: two oversized layers of pre-softened radial glows in the brand palette drifting on `transform` only — compositor-friendly, no filter animation, disabled under `prefers-reduced-motion`.
- **Seats are one click.** The credential form is hidden behind a `SHOW_LOGIN_FORM` flag (hidden, not removed — one line restores it): clicking a seat row signs in directly with the demo credentials, the row reads "Signing in…", and the per-role **"open in a new window"** link (`/?seat=<role>`) auto-enters the workspace in a fresh tab — the demo is meant to be watched from several seats at once.
- Account list, tuned over several rounds: 48px avatars, 16px names with relaxed line-height, CTAs revealed on hover/focus in the same pale voice as the new-window icon, darker hover fills, whole-card hover on the open slot, and (for when the form returns) an unfold with real height — `grid-template-rows: 0fr → 1fr` from `@starting-style` plus a dissolve.
- Footer: demo note and custodian note merged into one two-line paragraph, Reset demo bottom-right, bottoms aligned with the brand panel's small print.

## 5. Motion system (emil-design-eng principles)

- **Entrances are pure dissolves** — no vertical travel anywhere (`rise-in` → `fade-in`, toasts included). Staggered per-section delays.
- **Asymmetry with intent**: the inspector drawer enters slowly (500ms iOS-drawer curve) and leaves fast (200ms strong ease-out), strictly right→left, no fade, no shadow, backdrop lands instantly — the slide stays on the compositor. Sign-out dissolves the whole workspace (260ms) before the session drops, so the swap to sign-in reads as a crossfade.
- **Toasts**: a plain column, newest at the bottom; smooth fade-in, longer lifetimes (9s success / 14s error), a draining countdown hairline per toast, hover pauses both the JS timer and the CSS bar in lockstep, dismissal is a fade, not a pop — and a **"Dismiss all"** control appears with the second toast (pinned by a test).
- **Loading**: native-feeling preloaders (breathing dash in real layout, not gray skeleton blocks), a 500ms minimum display floor so first paint doesn't pop, and a one-time dissolve when figures land.

### 5a. Systematic animation review

A final pass audited every `transition` and `animation` in the system against the design-engineering checklist (easing intent, duration bands, entrance rules, press feedback, reduced motion). Fixed:

| Before | After | Why |
| --- | --- | --- |
| `.dialog-backdrop { animation: toast-in 140ms }` — dim and panel faded as one | Backdrop: pure fade 180ms; `.dialog`: `dialog-in 220ms var(--ease-out)` from `scale(0.97)` | A modal scales from centre, never from zero; the panel carries the motion, not the dim |
| `settled-in 0.35s ease` | `settled-in 0.32s var(--ease-out)` | Built-in `ease` is weak; entrances ride the strong ease-out |
| Settlement leg sweeps on generic `ease` | fill/fail → `var(--ease-out)`, revert → new `--ease-in-out` token `cubic-bezier(0.77, 0, 0.175, 1)` | On-screen movement wants in-out; progress fills want ease-out |
| `.icon-btn`, `.choice`, `.hash-chip`, `.toast-inspect`, `.toast-clear`, `.sl-cap`, `.acc-newwin` had no `:active` state | `:active { transform: translateY(1px) }` on all | Every pressable answers the press, like `.btn` |
| `.skip-link` revealed with `ease` | `ease-out` | A focus reveal is an entrance |
| Reduced-motion list missed the new animations | `.dialog` added to the reduced-fade set; press transforms disabled | Coverage grows with the system |

Deliberately left as-is: tab switching is instant (a many-times-a-day action — no animation), the spinner spins fast (0.7s — perceived speed), toast countdowns are linear (constant motion), the drawer keeps its 500ms-in / 200ms-out asymmetry, and section entrances run 500ms because they are page-level dissolves, not UI controls.

## 6. Mobile and responsive

- **≤960px the sidebar dissolves into an app frame**: one fixed translucent header row (logo + horizontally scrollable section tabs, backdrop blur) and the identity/custodian footer at the very foot of the page. `display: contents` + flex order does the re-arrangement without duplicating DOM.
- Scroll resets on entering the shell and on section change — previously the sign-in accordion's `scrollIntoView` survived the SPA swap and the workspace opened 550px down.
- Ancillary fixes: `scrollbar-gutter: stable` (no sideways shell jump between pages), stacked page header under 1240px with the stepper in its own scroll lane, 2×1 KPI grid, single-column forms/kv tables, safe-area padding on the mobile footer.

## 7. Anchored documents in the house style (`7a482d1`)

The three served documents (`valuation-report`, `fairness-opinion`, `purchase-agreement`) were paper-white letters with three different accent colors. They now share the workspace's exact tokens and type — dark panel on dark ground, serif titles, pale uppercase section labels, primary-accent opinion callouts and NAV box, tabular numerals — with **content untouched**. Because the ledger anchors the **sha256 of the served bytes**, `manifest.json` and `hashes.ts` were regenerated in the same commit, keeping `GET /verify/:name` honest. (A running sandbox needs a restart + Reset demo to re-anchor with the new hashes.)

## 8. Pitch deck (`ddc9aee`)

- "How it works" now carries the close's real numbers ($500M NAV, 96%, $288M cash / 288M units / 192M rolled units) instead of placeholders.
- The Demo slide is self-contained: seats, custodians, and the five-window SETTLED money-shot explained without needing the app open.
- The deck keeps its own visual system on purpose — it is a marketing artifact, not the product.

## 9. Brand details

- Logo enlarged (19px sidebar / 22px sign-in), a small `v0.1` tag beside it, and the infinity mark now carries a **flowing gradient** (SMIL `gradientTransform` loop inside the SVG — palette unchanged, works inside `<img>`), while the wordmark stays solid white. Favicon from the supplied `fav.svg`.
- Six supplied portraits wired as seat avatars across sign-in, sidebars, and identity rows.

## 10. Merge with `main` (94d10b7)

While the redesign was in flight, `main` picked up a simplification pass on the *old* interface — trimmed copy, the Trust Model removed, a fixed seat-switch bar added. The branch merges `main` with the conflicts resolved to the redesign, each resolution deliberate:

- **Trust Model stays** — authorization-without-browser-keys is the product's story; it lives as a quiet sidebar dialog rather than removed chrome.
- **Stage-aware guidance stays** — the audit specifically replaced stale instructions ("Ready: open elections" shown while elections are already open).
- **The fixed seat-switch bar was not adopted** — single-window seat pivoting undersells the multi-window atomic close; one-click seat entry and per-role new-window links cover the operator's need without a seventh piece of chrome. Worth revisiting if a single-window pitch mode is wanted.
- Dead CSS from the superseded pass (`.seat-bar`, `.status-line`) removed; PR [#3](https://github.com/kmadorin/continuum-prototype/pull/3) is mergeable and green.

## 11. How it was verified

- **Unit/integration**: 74 web tests across 24 files (lifecycle derivation, oversight-on-markers, projection non-leak pins, shell rendering, toast dismiss-all) and 37 backend tests — green after every batch.
- **End-to-end**: full deal click-through in the sandbox from every seat — anchor → consent → bids → elections → allocation → atomic Close — checking that each seat's settled holdings equal what its screen promised, and that all five windows show the same receipt id.
- **Visual**: every polish item was screenshot-verified with Playwright against the running sandbox at 1440px and 390px before being called done.

## 12. Open items

| Item | Status |
| --- | --- |
| LPAC valuation visibility (Daml observer + redeploy) | Deferred by decision |
| Deal naming (Meridian CV I vs Project Continuum CV I, L.P.) | Deferred by decision |
| Activity feed: no close entry | Open, cosmetic |
| `LPACConsentRequest → Grant` path unreachable | Open, cosmetic |
| Credential form hidden (`SHOW_LOGIN_FORM = false`) | Deliberate demo simplification — one line restores the inline login |
